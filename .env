# .env
# Cấu hình biến môi trường cho dự án Build Resume AI

# Nhà cung cấp và model của LLM
LLM_PROVIDER="google"           # provider mặc định: 'google'. Nếu dùng OpenRouter, đổi thành 'openrouter'
LLM_MODEL="gemini-2.0-flask"  # model mặc định, ví dụ 'gemini-2.0-flask'

# Khóa API cho Google AI Studio (Gemini Free)
GOOGLE_API_KEY="AIzaSyDAAMBYciyO_aOAnBNNCdtPVOIx8LMGm1M"   # API key Google (lấy từ Google Cloud Console)

# Khóa API cho OpenRouter (chỉ dùng khi LLM_PROVIDER=openrouter)
OPENROUTER_API_KEY="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"    # API key OpenRouter, định dạng 'sk-or-…'
MCP_API_KEY=""                   # API key cho MCP server (tùy platform)

# Cấu hình IMAP để fetch CV từ email
EMAIL_HOST="imap.gmail.com"     # địa chỉ server IMAP, ví dụ 'imap.gmail.com'
EMAIL_PORT=993               # cổng IMAP SSL, mặc định 993
EMAIL_USER="vuhuudo2004@gmail.com"  # tên đăng nhập email
EMAIL_PASS="jryv ejyi blsr sgcv"     # mật khẩu hoặc app-password email

# Thư mục lưu attachments và file kết quả
ATTACHMENT_DIR="attachments"    # thư mục lưu file CV đính kèm
OUTPUT_CSV="cv_summary.csv"     # tên file CSV xuất kết quả

# === HƯỚNG DẪN ===
# 1. Google API Key: Lấy từ https://aistudio.google.com/app/apikey
# 2. OpenRouter API Key: Lấy từ https://openrouter.ai/keys
# 3. Email App Password: Tạo app password cho Gmail tại https://myaccount.google.com/apppasswords

# === KIỂM TRA MODELS KHẢ DỤNG ===
# Chạy lệnh sau để xem danh sách models được lấy từ API:
# python config_info.py
# python test_models.py

# === LƯU Ý ===
# - Models sẽ được tự động fetch từ API khi khởi động
# - Nếu API call thất bại, sẽ sử dụng fallback models
# - Có thể thay đổi LLM_MODEL thành bất kỳ model nào trong danh sách
